[pytest]
testpaths = tests
markers =
    e2e: end-to-end tests requiring a running server and/or Selenium
    slow: tests that take more than a few seconds

# Default: run fast tests only (ignore e2e subfolder)
addopts = --ignore=tests/e2e -q --timeout=60
timeout = 60
