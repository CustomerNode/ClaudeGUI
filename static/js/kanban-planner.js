/**
 * kanban-planner.js — Stub. Planning is handled by createTask() popup
 * + _openPlannerSlideout() in kanban.js.
 */
function openAiPlanner() { createTask('not_started'); }
function closeAiPlanner() { _closePlannerSlideout(); }
